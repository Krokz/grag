# grag UI

Dev: `npm install && npm run dev` (proxies `/api` → `http://127.0.0.1:8471` — run `grag serve` first).
Build: `npm run build` → bundles to `../src/grag/api/static/`, served by the FastAPI app at `/`.
